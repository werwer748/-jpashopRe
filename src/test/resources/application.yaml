spring:
#  datasource:
#    url: jdbc:h2:mem:test # url을 mem으로 써도 되지만 기본적으로 아무 설정이없으면 스프링이 알아서 메모리모드로 실행
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.orm.jdbc.bind: trace